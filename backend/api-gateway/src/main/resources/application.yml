server:
  port: 8083

spring:
  application:
    name: decky
  cloud:
    gateway:
      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://localhost:5173"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        - id: user-auth
          uri: ${USER_AUTH_URI:http://localhost:8081}
          predicates:
            - Path=/auth/**
            
        - id: deck-service
          uri: ${DECK_SERVICE_URI:http://localhost:8080}
          predicates:
            - Path=/api/flashcards/**
          filters:
            - StripPrefix=1
            
        - id: today-session-service
          uri: ${TODAY_SESSION_SERVICE_URI:http://localhost:8082}
          predicates:
            - Path=/api/sessions/**
          filters:
            - StripPrefix=1

jwt:
  secret: ${JWT_SECRET:default_secret_for_local_dev}